let lista = []
let total = 0
a => a+2 
    function calcularTotal(lista) {
        x = lista.map()
        for (let item of lista) {
            if (item === 'Banana'){
                total += 2;
            } else if (item === 'Maça'){
                total += 3;
            } else if (item === 'Laranja'){
                total += 2,5;
            } 
        };
    return total;
    };
console.log(`R$${calcularTotal(['Maça', 'Banana', 'Banana', 'Uva']).toFixed(2)}`);


